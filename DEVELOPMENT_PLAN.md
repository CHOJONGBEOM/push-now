# PushNow MVP 개발 계획서

## 현재 상태 분석

### 완료된 것
| 항목 | 상태 | 파일 |
|------|------|------|
| 기본 레이아웃 | ✅ | App.tsx, Sidebar.tsx, Header.tsx |
| 히트맵 컴포넌트 | ✅ (더미 데이터) | Heatmap.tsx |
| 요일별 차트 | ✅ (더미 데이터) | VolumeChart.tsx |
| 트렌드 차트 | ✅ (더미 데이터) | TrajectoryChart.tsx |
| Tailwind 스타일링 | ✅ | 전체 |
| 반응형 모바일 메뉴 | ✅ | App.tsx |

### 미완료 (필요한 작업)
| 항목 | 우선순위 | 비고 |
|------|----------|------|
| 패키지 설치 | P0 | supabase-js, react-router-dom |
| Supabase 연동 | P0 | 실제 데이터 연결 |
| 라우팅 설정 | P1 | 페이지 구조 |
| 실시간 피드 | P1 | MVP 핵심 기능 #1 |
| 시간 히트맵 | P1 | MVP 핵심 기능 #2 |
| 키워드 분석 | P2 | MVP 핵심 기능 #3 |
| 한국어 UI | P2 | 타겟 사용자 맞춤 |

### 현재 코드 문제점
1. **영문 UI** - "Strategy Analytics", "Executive Summary" 등 → 한국어로 변경 필요
2. **더미 데이터** - 모든 차트가 하드코딩된 데이터 사용
3. **단일 페이지** - 라우터 없음, 모든 컴포넌트가 App.tsx에 집중
4. **PushNow 브랜딩 없음** - "Premium Analytics" → "PushNow"로 변경 필요

---

## 개발 단계

### Phase 0: 환경 설정 (Day 1)

#### 0-1. 패키지 설치
```bash
npm install @supabase/supabase-js react-router-dom
npm install -D @types/react-router-dom
```

#### 0-2. 폴더 구조 정리
```
src/
├── config/
│   └── supabase.ts          # Supabase 클라이언트
├── hooks/
│   ├── usePushMessages.ts   # 푸시 메시지 조회 훅
│   └── useRealtimeFeed.ts   # 실시간 구독 훅
├── types/
│   └── index.ts             # 타입 정의
├── pages/
│   ├── Dashboard.tsx        # 대시보드 (메인)
│   ├── Feed.tsx             # 실시간 피드
│   └── Analytics.tsx        # 분석 (히트맵)
├── components/
│   ├── layout/
│   │   ├── Sidebar.tsx
│   │   └── Header.tsx
│   ├── feed/
│   │   ├── FeedCard.tsx     # 푸시 메시지 카드
│   │   └── FeedFilter.tsx   # 필터 UI
│   ├── charts/
│   │   ├── Heatmap.tsx
│   │   ├── VolumeChart.tsx
│   │   └── KeywordChart.tsx
│   └── common/
│       ├── AppIcon.tsx      # 앱 아이콘 (브랜드 컬러)
│       └── CategoryBadge.tsx
├── utils/
│   ├── dateUtils.ts         # 날짜 포맷팅
│   └── keywordExtractor.ts  # 키워드 추출
├── App.tsx
└── index.tsx
```

#### 0-3. Supabase 설정
```typescript
// src/config/supabase.ts
import { createClient } from '@supabase/supabase-js';
import type { Database } from '../types/database';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

export const supabase = createClient<Database>(supabaseUrl, supabaseKey);
```

---

### Phase 1: 핵심 기능 구현 (Day 2-4)

#### 1-1. 타입 정의
```typescript
// src/types/index.ts
export interface PushMessage {
  id: number;
  package_name: string | null;
  app_name: string | null;
  title: string | null;
  body: string | null;              // ※ content 대신 body 사용
  posted_at: string | null;
  collected_at: string | null;
  is_ad: boolean | null;
  category: 'sale' | 'new' | 'restock' | 'reminder' | 'event' | 'other' | null;
  has_emoji: boolean | null;
  message_length: number | null;
  created_at: string | null;
  updated_at: string | null;
  is_hidden: boolean | null;
}

export interface AppInfo {
  name: string;
  packageName: string;
  color: string;
  icon?: string;
}

export const APP_COLORS: Record<string, string> = {
  '무신사': '#000000',
  '지그재그': '#FF6B9D',
  '29CM': '#00C4B4',
  '에이블리': '#FF5722',
  'W컨셉': '#000000',
  '컬리': '#5F0080',
  'N+스토어': '#03C75A',
  '마이리얼트립': '#2B96ED',
  'NOL(야놀자)': '#FF3478',
  // ... 추가
};
```

#### 1-2. 실시간 피드 구현
**목표**: 경쟁사 푸시를 실시간 타임라인으로 표시

```typescript
// src/hooks/useRealtimeFeed.ts
export function useRealtimeFeed() {
  const [messages, setMessages] = useState<PushMessage[]>([]);
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    // 초기 데이터 로드 (최근 24시간)
    fetchRecentMessages();

    // 실시간 구독
    const subscription = supabase
      .channel('push_messages')
      .on('postgres_changes',
        { event: 'INSERT', schema: 'public', table: 'push_messages' },
        (payload) => {
          setMessages(prev => [payload.new as PushMessage, ...prev]);
        }
      )
      .subscribe();

    return () => {
      subscription.unsubscribe();
    };
  }, []);

  return { messages, isLoading };
}
```

**UI 컴포넌트**:
- `FeedCard.tsx`: 개별 푸시 메시지 카드
  - 앱 아이콘 (브랜드 컬러)
  - 카테고리 뱃지
  - 제목 + 본문
  - 발송 시간 (상대 시간: "방금 전", "12분 전")
- `FeedFilter.tsx`: 앱/카테고리 필터

#### 1-3. 시간대 히트맵 구현
**목표**: 요일 × 시간대 매트릭스로 발송 밀도 시각화

```typescript
// src/hooks/useHeatmapData.ts
export function useHeatmapData(days: number = 30) {
  // Supabase에서 집계 쿼리
  // GROUP BY EXTRACT(DOW FROM posted_at), EXTRACT(HOUR FROM posted_at)

  return {
    data: HeatmapCell[][],  // 7일 × 24시간
    recommendations: TimeSlot[],  // 추천 시간대 TOP 3
    isLoading: boolean
  };
}
```

**추천 로직**:
```typescript
// 경쟁 밀도가 낮은 시간대 = 추천 시간대
function findGoldenHours(data: HeatmapCell[][]): TimeSlot[] {
  const allSlots = data.flat();
  const avgCount = average(allSlots.map(s => s.count));

  return allSlots
    .filter(s => s.count < avgCount * 0.7)  // 평균의 70% 미만
    .sort((a, b) => a.count - b.count)
    .slice(0, 3);
}
```

---

### Phase 2: UI/UX 개선 (Day 5-6)

#### 2-1. 한국어 UI 적용
| Before (영문) | After (한국어) |
|--------------|---------------|
| Strategy Analytics | 푸시 분석 대시보드 |
| Executive Summary | 오늘의 인사이트 |
| Market Dynamics | 실시간 피드 |
| Pattern Analysis | 시간 분석 |
| Competitive Edge | 키워드 트렌드 |
| Optimal Window | 추천 발송 시간 |

#### 2-2. 브랜딩 변경
- 로고: "Premium Analytics" → "PushNow"
- 태그라인: "경쟁사 푸시를 놓치지 마세요"
- 색상: 기존 흑백 베이스 유지

#### 2-3. 앱 아이콘 컴포넌트
```typescript
// src/components/common/AppIcon.tsx
export function AppIcon({ appName, size = 'md' }: Props) {
  const color = APP_COLORS[appName] || '#6B7280';

  return (
    <div
      className={`rounded-lg flex items-center justify-center ${sizeClasses[size]}`}
      style={{ backgroundColor: color }}
    >
      <span className="text-white font-bold">
        {appName.charAt(0)}
      </span>
    </div>
  );
}
```

#### 2-4. 카테고리 뱃지
```typescript
const CATEGORY_CONFIG = {
  sale: { label: '할인', color: 'bg-red-100 text-red-700', icon: '🏷️' },
  new: { label: '신상', color: 'bg-blue-100 text-blue-700', icon: '🆕' },
  restock: { label: '재입고', color: 'bg-green-100 text-green-700', icon: '📦' },
  reminder: { label: '리마인더', color: 'bg-yellow-100 text-yellow-700', icon: '🔔' },
  event: { label: '이벤트', color: 'bg-purple-100 text-purple-700', icon: '🎁' },
  other: { label: '기타', color: 'bg-gray-100 text-gray-700', icon: '📌' },
};
```

---

### Phase 3: 키워드 분석 (Day 7-8)

#### 3-1. 키워드 추출 로직
```typescript
// src/utils/keywordExtractor.ts
const MARKETING_KEYWORDS = [
  '무료배송', '한정수량', '오늘만', '단독', '특가', '세일',
  '할인', '쿠폰', '적립', '신상', '재입고', '품절임박',
  '선착순', '마감임박', '최대', '최저가', '혜택'
];

export function extractKeywords(text: string): string[] {
  return MARKETING_KEYWORDS.filter(keyword =>
    text.includes(keyword)
  );
}
```

#### 3-2. 키워드 집계 쿼리
```sql
-- 최근 7일 키워드 빈도
WITH keywords AS (
  SELECT
    unnest(extracted_keywords) as keyword,
    DATE(posted_at) as date
  FROM push_messages
  WHERE posted_at > NOW() - INTERVAL '7 days'
)
SELECT
  keyword,
  COUNT(*) as count,
  COUNT(*) FILTER (WHERE date = CURRENT_DATE) as today_count
FROM keywords
GROUP BY keyword
ORDER BY count DESC
LIMIT 10;
```

---

## 라우팅 구조

```typescript
// src/App.tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Navigate to="/dashboard" replace />} />
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/feed" element={<Feed />} />
        <Route path="/analytics" element={<Analytics />} />
        <Route path="/keywords" element={<Keywords />} />
      </Routes>
    </BrowserRouter>
  );
}
```

---

## 페이지별 구성

### 1. Dashboard (`/dashboard`)
메인 랜딩 페이지 - 핵심 지표 요약

```
┌─────────────────────────────────────────────────────────┐
│  [Sidebar]  │  PushNow                    🔍  👤        │
│             ├───────────────────────────────────────────│
│  실시간피드  │                                          │
│  시간분석   │  오늘의 인사이트                           │
│  키워드     │  ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│             │  │ 오늘 수집 │ │ 가장 활발 │ │ 추천시간 │  │
│             │  │   47건   │ │  무신사   │ │ 화 10시  │  │
│             │  └──────────┘ └──────────┘ └──────────┘  │
│             │                                          │
│             │  최근 푸시 미리보기 (5건)                  │
│             │  ┌────────────────────────────────────┐  │
│             │  │ [무신사] 단 3일! 인기 브랜드 70%... │  │
│             │  │ [29CM] 2월 신상 컬렉션이 도착...    │  │
│             │  └────────────────────────────────────┘  │
│             │                                          │
│             │  시간대 히트맵 (미니)                      │
│             │  ┌────────────────────────────────────┐  │
│             │  │ ░░▓▓██░░░░▓▓░░░░░░██▓▓░░░░        │  │
│             │  └────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2. Feed (`/feed`)
실시간 푸시 타임라인

```
┌─────────────────────────────────────────────────────────┐
│  [Sidebar]  │  실시간 피드                오늘 47건     │
│             ├───────────────────────────────────────────│
│             │  필터: [전체 앱 ▼] [전체 카테고리 ▼]      │
│             │                                          │
│             │  ┌────────────────────────────────────┐  │
│             │  │ ■ 무신사              방금 전      │  │
│             │  │ 🏷️ 할인                            │  │
│             │  │ 단 3일! 인기 브랜드 최대 70% OFF    │  │
│             │  └────────────────────────────────────┘  │
│             │  ┌────────────────────────────────────┐  │
│             │  │ ■ 29CM                12분 전      │  │
│             │  │ 🆕 신상                            │  │
│             │  │ 2월 신상 컬렉션이 도착했어요        │  │
│             │  └────────────────────────────────────┘  │
│             │  ... (무한 스크롤)                       │
└─────────────────────────────────────────────────────────┘
```

### 3. Analytics (`/analytics`)
시간대별 발송 패턴 분석

```
┌─────────────────────────────────────────────────────────┐
│  [Sidebar]  │  시간 분석                               │
│             ├───────────────────────────────────────────│
│             │  🎯 추천 발송 시간                        │
│             │  ┌────────────────────────────────────┐  │
│             │  │ 1위: 화요일 10:00 (경쟁 밀도 -42%) │  │
│             │  │ 2위: 목요일 14:00 (경쟁 밀도 -38%) │  │
│             │  │ 3위: 일요일 20:00 (경쟁 밀도 -35%) │  │
│             │  └────────────────────────────────────┘  │
│             │                                          │
│             │  시간대별 히트맵                          │
│             │  ┌────────────────────────────────────┐  │
│             │  │     00  04  08  12  16  20  24     │  │
│             │  │ 월  ░░  ░░  ▓▓  ██  ▓▓  ░░  ░░     │  │
│             │  │ 화  ░░  ░░  ░░  ▓▓  ▓▓  ██  ░░     │  │
│             │  │ ...                                │  │
│             │  └────────────────────────────────────┘  │
│             │                                          │
│             │  요일별 발송량                            │
│             │  ┌────────────────────────────────────┐  │
│             │  │ 월 ████                            │  │
│             │  │ 금 ████████████ (피크)             │  │
│             │  └────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 일정 요약

| Day | Phase | 작업 내용 |
|-----|-------|----------|
| 1 | Phase 0 | 패키지 설치, 폴더 구조, Supabase 설정, 타입 정의 |
| 2 | Phase 1-1 | 실시간 피드 - 데이터 훅 + FeedCard 컴포넌트 |
| 3 | Phase 1-2 | 실시간 피드 - 필터, 무한 스크롤, 실시간 구독 |
| 4 | Phase 1-3 | 시간 히트맵 - 집계 쿼리 + 히트맵 컴포넌트 |
| 5 | Phase 2-1 | 한국어 UI, 브랜딩 변경 |
| 6 | Phase 2-2 | 대시보드 페이지 조합, 라우팅 완성 |
| 7 | Phase 3-1 | 키워드 추출 로직, 집계 쿼리 |
| 8 | Phase 3-2 | 키워드 차트, 예시 푸시 목록 |

---

## 기술적 고려사항

### 성능 최적화
1. **무한 스크롤**: 피드에서 대량 데이터 처리
2. **메모이제이션**: 히트맵 계산 결과 캐싱
3. **Supabase 인덱스**: `posted_at`, `app_name`에 인덱스 필요

### 에러 처리
1. **네트워크 오류**: 재시도 로직, 오프라인 상태 표시
2. **빈 데이터**: "데이터가 없습니다" 빈 상태 UI
3. **로딩 상태**: 스켈레톤 UI

### 테스트 데이터
- Supabase에 실제 데이터가 있다고 가정
- 없을 경우 목업 데이터로 UI 테스트 가능하도록 구성

---

## 시작 명령어

```bash
# 1. 패키지 설치
npm install @supabase/supabase-js react-router-dom

# 2. 환경변수 설정 (.env.local)
VITE_SUPABASE_URL=your_url
VITE_SUPABASE_ANON_KEY=your_key

# 3. 개발 서버 시작
npm run dev
```

---

**문서 버전**: 1.0
**작성일**: 2026-02-09
**다음 단계**: Phase 0 환경 설정부터 시작
